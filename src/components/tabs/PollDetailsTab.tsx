import ForgeUI, { Fragment, Text } from "@forge/ui";

export default function PollDetailsTab({ data }) {
  console.log("data", data);
  return (
    <Fragment>
      <Text>PollDetailsTab</Text>
    </Fragment>
  );
}
